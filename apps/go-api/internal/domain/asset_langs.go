package domain

// TargetLanguages contains the language requested from the discovery API.
var TargetLanguages = []string{"en-US"}

// DefaultLanguage is the fallback for assets without a localized name.
const DefaultLanguage = "en-US"
